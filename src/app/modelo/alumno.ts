import { NumberSymbol } from '@angular/common';

export interface Alumno{
  idalumno: number;
  nombre: string;
  direccion: string;
  escuela: number;
}
export interface Escuela {
  idescuela: number;
  nombre: string;
}
